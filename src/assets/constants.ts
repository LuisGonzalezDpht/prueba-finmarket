import type { rangeDate } from './type/range.type'

const constants = {
  default: 'ipsa',
  btn_dates: ['1M', '3M', '6M', '1A'] as rangeDate[],
  tabs_defaults: ['ipsa', 'igpa', 'nasdaq', 'dow jones', 'sp/bvl'],
}

export default constants
